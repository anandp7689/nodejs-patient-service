apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: appointment-service
spec:
  hosts:
  - "*"
  gateways:
  - patient-gateway   # ✅ Same gateway
  http:
  - match:
    - uri:
        prefix: /appointments
    route:
    - destination:
        host: appointment-service
        port:
          number: 80
